# ---- Web framework / server ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # multipart form parsing for /upload-image
websockets==14.1

# ---- Config / validation ----
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# ---- AI agent ----
langgraph==0.2.60
langchain==0.3.14
langchain-core==0.3.29
langchain-openai==0.2.14
openai==1.59.6

# ---- Data / infra ----
supabase==2.11.0                # Supabase Python client (PostgREST + Storage)
redis==5.2.1                    # pub/sub for admin -> websocket + payment -> websocket
httpx==0.27.2                   # async HTTP (Razorpay REST, image fetch)

# ---- Payments ----
razorpay==1.4.2                 # official Razorpay Python SDK (payment links + signature verify)
